# Bolt Repo Skills

- `bolt-scoping`: Use `withContainer` and `withOverrides` correctly.
- `bolt-modules-validation`: Validate `DependencyModule` graphs with `BoltValidator`.
- `bolt-testing`: Update Bolt behavior safely with focused tests and docs.
